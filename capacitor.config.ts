import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'realestatecare-prototype',
  webDir: 'dist'
};

export default config;
